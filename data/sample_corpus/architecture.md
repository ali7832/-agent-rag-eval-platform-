# Architecture

The Agent RAG Evaluation Platform is designed to ingest documents, chunk them, retrieve grounded evidence, and evaluate answer quality.

The system is intentionally modular so retrieval, generation, evaluation, and orchestration components can evolve independently.
