services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      RAG_ENVIRONMENT: local
      RAG_DATABASE_PATH: data/app.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
