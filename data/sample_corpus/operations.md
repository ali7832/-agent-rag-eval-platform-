# Operations

The service is designed for local deployability with SQLite persistence, a FastAPI interface, Docker packaging, and lightweight CI validation.

The next production-minded improvements are dense retrieval, reranking, orchestration, and observability.
